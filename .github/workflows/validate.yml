name: Validate

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    # Catches breakage from new Home Assistant / HACS releases.
    - cron: "0 4 * * 1"
  workflow_dispatch:

jobs:
  hassfest:
    name: hassfest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: home-assistant/actions/hassfest@master

  hacs:
    name: HACS
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: hacs/action@main
        with:
          category: integration
          # fints_atruvia is not registered in home-assistant/brands, so the
          # brands check can never pass here. Drop this once a brands PR lands.
          ignore: brands

  # No ruff job yet: `ruff check .` currently reports ~427 pre-existing findings
  # (select = ALL). Add it once that backlog is worked off, otherwise every run
  # is red and nobody looks at it.

  tests:
    name: pytest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v9
      - run: uv venv --python 3.14
      - run: uv pip install -r requirements.txt
      - run: PYTHONPATH=$PWD .venv/bin/pytest
