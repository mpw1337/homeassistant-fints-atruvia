[project]
name = "fints_atruvia"
version = "0.3.0"
requires-python = ">=3.14"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.coverage.run]
source = ["custom_components.fints_atruvia"]
